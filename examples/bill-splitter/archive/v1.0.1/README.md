# Historical bill-splitter v1.0.1 source export

The ZIP and report in this directory retain the original published bytes.
This is a `compatible-source` Draft with `.claude-plugin/plugin.json`, not a
current native Microsoft Copilot Cowork package or an installation fallback.

The [current candidate and blocked packaging status](../../README.md) require
Microsoft manifest v1.28 and approved app/publisher metadata. Do not replace
these historical files when building a newer package.
